---
driver:
  name: vagrant

provisioner:
  name: chef_zero
  # You may wish to disable always updating cookbooks in CI or other testing environments.
  # For example:
  #   always_update_cookbooks: <%= !ENV['CI'] %>
  always_update_cookbooks: true
  data_bags_path: ../../data_bags
  environments_path: ../../environments
  roles_path: ../../roles

verifier:
  name: inspec

platforms:
  - name: ubuntu-18.04

suites:
  - name: default
    run_list:
      - recipe[iptables::default]
      - recipe[git_server::default]
      - recipe[postgres_init::default]
      - recipe[tomcat_init::default]
      - recipe[gitenter_solo::default]
    verifier:
      inspec_tests:
        - test/integration/default
    attributes:
